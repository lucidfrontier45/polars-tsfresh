repos:
  - repo: local
    hooks:
      - id: format
        name: Format code project settings
        language: system
        entry: uv run poe format
        types: [python]
        pass_filenames: false
        always_run: true
